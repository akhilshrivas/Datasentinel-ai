[pytest]
markers =
    integration: marks tests as requiring real LLM or external services (deselect with '-m "not integration"')
addopts = -m "not integration"

filterwarnings =
    ignore:.*The anyio.abc.BlockingPortal alias is deprecated.*:DeprecationWarning:starlette.*
